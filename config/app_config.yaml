# GCP Configuration
#askvbc.askvbc_attribution.vbc_contracts
gcp:
  project_id: askvbc
  location: US

# BigQuery Configuration
bigquery:
  dataset: askvbc_attribution

# Application Settings
app:
  name: AskVBC
  environment: demo
  default_response: >
    I can help explain VBC attribution and delegation decisions
    for members using business-friendly language.